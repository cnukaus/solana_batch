<# 
requirement:
download and install Solana cli from https://docs.solana.com/cli/install-solana-cli-tools

 #>

# set seq between 1 and 26, use $to_letter to generate wallet file name from a-z
$cmd='solana-keygen'

$to_letter=[char](65+$seq)


#CAN'T have pipe in param
$cmd='solana-keygen'
 for ($seq=1; $seq -lt 5; $seq++)
{ 
 & "$cmd" "new","--no-bip39-passphrase", "-o","keys\key_$sum.json"| Out-File pubkeys.txt -Append

}